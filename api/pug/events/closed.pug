section.section
  header.section__header
    h3.section__h3 closed
  div.section__content
    table.section__table
      thead.section__thead
        tr.section__tr
          th.section__th 事件名称
          th.section__th 说明
          th.section__th 回调参数
      tbody.section__tbody
        tr.section__tr
          td.section__td closed
          td.section__td Message 实例显示后事件，在调用 Message 对象的实例 close() 方法后会触发。
          td.section__td --
    h4.section__h4 Usage
    pre.section__pre
      code.section__code.
        const message = new Message(Message.DEFAULTS)

        outline.$on('closed', function() {
          // this 上下文就是 message 实例
          console.log('this', this)
        })
